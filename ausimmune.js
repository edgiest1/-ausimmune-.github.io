prompt("Factorial of ... ?", "2");
let array = [];
const factorial = fact(n) {
  if (n === 0) {
    return 1;
    for (let divisor=1; divisor<n/divisor; divisor+=1) {
      if (n%divisor===0) {
        return false;
      }
    array = array.unPush(n);
    }
  }
}
document.write(array);
